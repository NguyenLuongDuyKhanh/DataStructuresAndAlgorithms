// Use any below method to implement Nodes of binary tree

// 1: Using struct
struct Node {
    int data;
    Node* left;
    Node* right; // store address
};

// { 2, 3, 4, 10, 40 }
leftnode = (
    data = 3;
    left
    right
)
rightnode = (
    data = 4
    left
    right
)
rootNode = (
    data = 2;
    left = leftnode;
    right = data
)