# We should investigate
The two nodes are adjacent or anywhere in the list.
One of them is the head.
Swap function require 2 values or two pointers:
- If true values, it cannot handle if it is same values.
- If two pointer, where will never be 2 same pointer.
